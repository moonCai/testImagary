import Vuex from 'vuex'
import Vue from 'vue'

Vue.use(Vuex);

// 需要维护的状态
const state = {
  // 选中时间
  selectTime: '',
};

const mutations = {
  selectTime(state, msg) {
    state.selectTime = msg;
  }
};

export default new Vuex.Store({
  state,
  mutations
});
